# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

/experiments

# dependencies
/node_modules
**/models
/.pnp
.pnp.*
# machine-local pnpm build-approval stub (pnpm 11 writes this; values differ per machine)
frontend/pnpm-workspace.yaml
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
*.mp4

# testing
/coverage
/dist

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env
# macOS signing/notarization secrets (template is .env.signing.example)
.env.signing
# Google Calendar OAuth client id/secret (specs/0032)
.env.google

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Audio files
*.wav

# Large media files
docs/demo.mov
docs/demo.gif


meeting_minutes.db

# Added by Task Master AI
# Logs
logs
*.log
dev-debug.log
# Dependency directories
node_modules/
# Environment variables
.env
# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
experiments/
.cursor/
target/

frontend/src-tauri/binaries/
# sherpa-onnx diarization dylibs copied here by build.rs (specs/0010); build artifacts
frontend/src-tauri/frameworks/
# design-sync (staged scripts + build output + machine state)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
frontend/.ds-sync-styles/

# Design records (Vinyl-era specs + implementation plans) — kept locally for reference,
# out of the public repository.
private/

# SDD scratch (ledgers, briefs, review packages)
.superpowers/

# screenshot pipeline diff sheet (local artefact, specs/0060)
docs/screenshots/diff/
